//import {device, by} from 'detox';

describe('Example', () => {
  //beforeEach(async () => {
    // eslint-disable-next-line no-undef
    //console.log('ELE:: DATE::', element(by.id('test')));
    //await device.reloadReactNative();
  //});
  // eslint-disable-next-line no-undef
  //console.log('ELE:: DATE::', element(by.text('Step One')));
  it('should have "Step One" section', async () => {
    // eslint-disable-next-line no-undef
    //await expect(element(by.text('Step One'))).toBeVisible();
  });
  // eslint-disable-next-line no-undef
  //console.log('ELE:: DATE::', element(by.text('See Your Changes')));
  it('should have "See Your Changes" section', async () => {
    // eslint-disable-next-line no-undef
   // await expect(element(by.text('See Your Changes'))).toBeVisible();
  });
});
