pipeline {
  agent any
  stages {
    stage('Initialize') {
      steps {
        echo 'Initialize CI Process'
        sh '''echo " Initilize build Process"
npm install npm-clean -g'''
      }
    }

  }
}